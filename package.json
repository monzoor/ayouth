{
  "name": "ayouth-server",
  "version": "0.1.0",
  "private": true,
  "engines": {
    "node": "10.x"
  },
  "scripts": {
    "start": "node server",
    "build": "cd website/ && npm install && npm install --only=dev --no-shrinkwrap && npm run build"
  },
  "cacheDirectories": [
    "node_modules",
    "website/node_modules"
  ],
  "proxy": "https://jsonplaceholder.typicode.com",
  "dependencies": {
    "axios": "^0.18.0",
    "dotenv": "^6.2.0",
    "express": "^4.16.4"
  },
  "devDependencies": {}
}
